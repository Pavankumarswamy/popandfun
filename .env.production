# Production environment for custom domain (https://popandfun.gnritservices.com)
# This file should be used when deploying to your custom domain with backend

# Backend API endpoint (update this with your actual backend URL)
VITE_API_ENDPOINT=https://popandfun.gnritservices.com/api

# Firebase Config (already in firebase.ts)
# Razorpay keys (in backend API only, not in frontend)
